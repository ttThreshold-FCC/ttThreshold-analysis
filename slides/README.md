# Slides: $m_W$ by forward-folding (MEM via MC reweighting)

Beamer deck explaining the 1-D $m_W$ fit for $WW\to\ell\nu q\bar q$ at the FCC-ee threshold:
the folded double-BW likelihood, why the analytic convolution fails, forward-folding (MEM by MC
reweighting), the two read-outs (ensemble + per-event), data/MC validation, results and systematics.

## Build
```bash
./build.sh                 # uses TeXLive 2024 from CVMFS; runs pdflatex twice
```
Output: `mw_forward_fold.pdf` (23 pages, 16:9).

## Files
- `mw_forward_fold.tex` — the deck.
- `figs/` — figures (copied from `/eos/user/m/mdefranc/www/mW/conv_mw/`; regenerated by the
  `jax_prototype/conv_mw_*.py` scripts).

Numbers/plots correspond to the state documented in
`~/.claude/projects/<hash>/notes/convolution_mw_fit/RESULTS.md` (2026-06-19).
